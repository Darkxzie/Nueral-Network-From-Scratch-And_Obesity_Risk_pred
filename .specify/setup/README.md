# Spec-Kit Setup

This repository uses lightweight Spec-Kit scaffolding.

- Put feature specs under `specs/<feature-name>/spec.md`.
- Capture implementation steps in `plan.md`.
- Track checklist state in `tasks.md`.

