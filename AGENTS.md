# Repository Notes

- Keep notebook artifacts under `notebooks/`.
- Use `submission_artifacts/` for Python helpers that support tests and repository validation.
- Update `specs/` when submission structure or compliance workflows change.
